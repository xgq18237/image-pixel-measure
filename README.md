# image-pixel-measure

本地化图片像素测距、取色、框选和 JSON 导出工具。

## 功能

- 选择本地图片文件夹，浏览器本地读取，不上传图片
- 查看像素坐标、点击取色、复制颜色值
- 添加刻度尺标线
- 框选图片区域并按网格导出数据
- 支持黑白主题切换
- 支持直接打开 `index.html` 使用

## 本地运行

直接双击 `index.html` 即可使用。

也可以用本地静态服务运行：

```bash
npm install
npm run dev
```

## 数据隐私

所有图片处理都在浏览器本地完成。工具不会上传图片或测量结果。
